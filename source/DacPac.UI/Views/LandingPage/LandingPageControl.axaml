<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:DacPac.UI.ViewModels.LandingPage"
             xmlns:converters="clr-namespace:DacPac.UI.Converters"
             x:DataType="viewModel:LandingPageControlViewModel"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
              x:Class="DacPac.UI.Views.LandingPage.LandingPageControl">
    <UserControl.Resources>
        <converters:ContainsConverter x:Key="ContainsConverter" />
    </UserControl.Resources>
    <UserControl.Styles>
        <Style Selector="Arc.spinner">
            <Setter Property="RenderTransformOrigin" Value="50%,50%" />
            <Style.Animations>
                <Animation Duration="0:0:0.9" IterationCount="Infinite" Easing="LinearEasing">
                    <KeyFrame Cue="0%">
                        <Setter Property="RotateTransform.Angle" Value="0" />
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="RotateTransform.Angle" Value="360" />
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>
    <Grid RowDefinitions="Auto,*" RowSpacing="12">
        
        <!-- Search bar -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="8">
            <TextBox Grid.Column="0"
                     PlaceholderText="Search (press enter to execute)…"
                     Classes="Search"
                     VerticalAlignment="Top"
                     Text="{Binding SearchText}">
                <TextBox.KeyBindings>
                    <KeyBinding Gesture="Enter" Command="{Binding SearchCommand}" />
                </TextBox.KeyBindings>
            </TextBox>
            <ComboBox Grid.Column="1"
                       MinWidth="160"
                      ToolTip.Tip="Filter options. Click All to toggle all or deselect all"
                      VerticalAlignment="Top"
                      PlaceholderText="{Binding FilterSummary}"
                      ItemsSource="{Binding FilterOptions}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="viewModel:FilterOption">
                        <CheckBox HorizontalAlignment="Stretch"
                                  Content="{Binding DisplayName}"
                                  Command="{Binding $parent[ComboBox].((viewModel:LandingPageControlViewModel)DataContext).ToggleFilterCommand}"
                                  CommandParameter="{Binding}">
                            <CheckBox.IsChecked>
                                <MultiBinding Converter="{StaticResource ContainsConverter}" Mode="OneWay">
                                    <Binding Path="Type" />
                                    <Binding Path="$parent[ComboBox].((viewModel:LandingPageControlViewModel)DataContext).SelectedFilters" />
                                </MultiBinding>
                            </CheckBox.IsChecked>
                        </CheckBox>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="2"
                      MinWidth="160"
                      VerticalAlignment="Top"
                      PlaceholderText="Schema"
                      ItemsSource="{Binding SchemaOptions}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="viewModel:ISchemaOption">
                        <CheckBox HorizontalAlignment="Stretch"
                                  Content="{Binding Display}"
                                  Command="{Binding $parent[ComboBox].((viewModel:LandingPageControlViewModel)DataContext).ToggleSchemaFilterCommand}"
                                  CommandParameter="{Binding}">
                            <CheckBox.IsChecked>
                                <MultiBinding Converter="{StaticResource ContainsConverter}" Mode="OneWay">
                                    <Binding />
                                    <Binding Path="$parent[ComboBox].((viewModel:LandingPageControlViewModel)DataContext).SelectedSchemaFilters" />
                                </MultiBinding>
                            </CheckBox.IsChecked>
                        </CheckBox>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="3"
                    Classes="Accent"
                    Content="Search"
                    ToolTip.Tip="Click to execute search"
                    VerticalAlignment="Top"
                    Command="{Binding SearchCommand}" />
            <Button Grid.Column="4"
                    Classes="Accent"
                    ToolTip.Tip="Open a file picker to select one or more dacpacs"
                    Content="Open"
                    VerticalAlignment="Top"
                    Command="{Binding OpenDacpacCommand}" />
            <Button Grid.Column="5"
                    Classes="Accent"
                    Content="Install"
                    ToolTip.Tip="Open an tab to install the database"
                    VerticalAlignment="Top"
                    Command="{Binding InstallCommand}" />
        </Grid>
    
        <!-- Results split -->
        <Grid Grid.Row="1" ColumnDefinitions="*,Auto,*">
            <DataGrid Grid.Column="0"
                      Name="ResultsGrid"
                      ItemsSource="{Binding FilteredResults}"
                      SelectedItem="{Binding SelectedResult}"
                      SelectionMode="Extended"
                      IsReadOnly="True"
                      AutoGenerateColumns="False"
                      CanUserResizeColumns="True">
                <DataGrid.Styles>
                    <Style Selector="DataGridRow" x:DataType="viewModel:SearchResultRow">
                        <Setter Property="Background" Value="{Binding GeneratorSupported, Converter={x:Static converters:GeneratorSupportedBrushConverter.Instance}}" />
                    </Style>
                </DataGrid.Styles>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Generate Code for all"
                                  ToolTip.Tip="Generates code for all the currently filtered items"
                                  Command="{Binding GenerateCodeCommand}" CommandParameter="{Binding FilteredResults}">
                            
                        </MenuItem>
                        <MenuItem Header="Generate Code"
                                  ToolTip.Tip="Generates code for the selected items that support generation"
                                  Command="{Binding GenerateCodeCommand}"
                                  CommandParameter="{Binding #ResultsGrid.SelectedItems}" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Database" Binding="{Binding Database}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                    <DataGridCheckBoxColumn IsReadOnly="True" Header="Can Generate Code" Binding="{Binding  GeneratorSupported}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="4" ResizeDirection="Columns" />

            <ContentControl  Grid.Column="2" Content="{Binding Detail}" />
            
            <!-- <TextBox Grid.Column="2" -->
            <!--          IsReadOnly="True" -->
            <!--          AcceptsReturn="True" -->
            <!--          TextWrapping="Wrap" -->
            <!--          VerticalContentAlignment="Top" -->
            <!--          Text="{Binding DetailsText}" /> -->
        </Grid>

        <!-- Loading overlay -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                IsVisible="{Binding IsLoading}"
                Background="#99000000">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16">
                <Arc Classes="spinner"
                     Width="48"
                     Height="48"
                     HorizontalAlignment="Center"
                     StartAngle="0"
                     SweepAngle="280"
                     Stroke="{DynamicResource SystemAccentColor}"
                     StrokeThickness="4"
                     StrokeLineCap="Round" />
                <TextBlock Text="{Binding LoadingMessage}"
                           HorizontalAlignment="Center"
                           Foreground="White" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
